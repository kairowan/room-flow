# room-flow 开发约定

修改本项目代码前，阅读 `.agents/skills/room-flow-development/SKILL.md`。
修复范围及验收见 `PLAN.md`，公开 API 用法及支持范围见 `README.md`。

遵循 ponytail 原则：先追踪调用链，再按“不必构建 → 已有代码 → 标准库 → 平台 → 已装依赖 → 最小实现”选择方案。
不新增未经请求的抽象、依赖、样板或全仓格式化。修共享根因，不给每个调用者打补丁。
非平凡逻辑必须留下最小可运行检查。`ponytail:` 注释说明有意简化、上限及升级方向。
安全、数据完整性、无障碍和实际设备验证不能作为简化牺牲项。
